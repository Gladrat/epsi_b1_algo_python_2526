c = "CCATTAGTTA"
print(c)

# On peut récupérer la longueur d'une string comme une liste
print(len(c))

# On peut interroger les index d'une string comme une liste
print(c[0])
print(c[-1])

# INTERDIT de modifier les strings comme une liste
c[0] = "Z"

# fonction qui renvoi la seq. comp.
    # ["CCATTAGTTA", "ATTGCCTTGG", "CGCTTGAAAT"]
    # retourner une string
    # parcourir la liste

# fonction qui renvoi "l'inverse" d'une string
    # affiche l'inverse de la string

    # 1. On prend une string en paramètre
    # 2. On affiche la string
    # 3. Avec une boucle on parcours chaque caractère de la string
        # 4. Avec des if/elif, on affiche le caractère inverse pour chaque caractère

# fonction qui parcours le tableau
    # affiche chaque élement du tableau